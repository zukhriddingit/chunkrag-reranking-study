# Reproduction boundaries

| Capability | Status | Evidence / missing dependency |
|---|---|---|
| View/check all included aggregate tables offline | Supported by the supplied standard-library tool | Hash verification and CSV-to-HTML formatting; no estimation |
| Compile manuscript/supplement | Previously tested in the separate private Prompt 11 package | Manuscript sources/PDFs are not in this candidate, avoiding unresolved human tables and declarations |
| Regenerate aggregate tables from saved summary inputs | Prior Prompt 11 check reproduced 23 CSV files exactly | Private synthesis dependencies and human table deliberately excluded here; not claimed self-contained |
| Recalculate primary inference from original saved outputs | Not supported by this payload | Restricted per-document/question outcomes and full original analysis dependency set missing |
| Recreate model experiment | Not supported or tested | Private training groups/splits/conversions, datasets, complete executed repairs, weights/adapters and provisioned runtime required |

The Phase 4 source directory is the pre-execution local preparation bundle, verified against its 20-file inventory. Runtime repair histories and executed-source hashes are retained separately; these hashes do not make the missing source bytes present. Selected Phase 3 analysis/correction sources are likewise not a standalone installation. No environment resolver, GPU, package installation or model import was run in this packaging stage.

The environment JSON lists observed packages. It is not a tested lockfile; the documented incompatible unused torchao exclusion must not be lost. Model IDs/revisions identify upstream dependencies; no weights or adapters are redistributed.

Finalization preserved exactly seven 163-byte AppleDouble sidecars only when each matched the recorded sidecar hash and had the exact sealed scientific sibling. No wildcard sidecar exception is permitted. The earlier failed repair and operational v3 repair remain disclosed in the selected ledger; science was not recomputed. Original 17-hash, 28-test and 984-file checks are reused, not rerun here.
